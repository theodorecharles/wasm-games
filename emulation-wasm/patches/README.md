# Downstream patches

Keep each core's browser changes in its named directory. Patches must be scoped
to this repository, guarded for Emscripten where practical, and described in
the runbook. They are not submitted upstream.

No patch imports an upstream web frontend or generated web artifact.
