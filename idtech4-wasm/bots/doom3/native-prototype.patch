diff --git a/neo/CMakeLists.txt b/neo/CMakeLists.txt
index 5b7f465..0b61baa 100644
--- a/neo/CMakeLists.txt
+++ b/neo/CMakeLists.txt
@@ -916,6 +916,7 @@ if(DEDICATED)
 endif()
 
 if(BASE)
+	list(APPEND src_game game/bots/BotAI.cpp game/bots/BotSabot.cpp game/bots/BotAASBuild.cpp)
 	if (AROS)
 		add_executable(base sys/aros/dll/dllglue.c ${src_game})
 		set_target_properties(base PROPERTIES OUTPUT_NAME "base.aros-${AROS_ARCH}")
@@ -926,7 +927,7 @@ if(BASE)
 	source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} PREFIX neo FILES ${src_game})
 	
 	set_target_properties(base PROPERTIES PREFIX "")
-	set_target_properties(base PROPERTIES COMPILE_DEFINITIONS "GAME_DLL")
+	set_target_properties(base PROPERTIES COMPILE_DEFINITIONS "GAME_DLL;MOD_BOTS")
 	target_include_directories(base PRIVATE "${CMAKE_SOURCE_DIR}/game")
 	set_target_properties(base PROPERTIES LINK_FLAGS "${ldflags}")
 	set_target_properties(base PROPERTIES INSTALL_NAME_DIR "@executable_path")
diff --git a/neo/game/Game_local.cpp b/neo/game/Game_local.cpp
index 41f1176..bae355a 100644
--- a/neo/game/Game_local.cpp
+++ b/neo/game/Game_local.cpp
@@ -48,6 +48,9 @@ If you have questions concerning this license or the applicable additional terms
 #include "Trigger.h"
 
 #include "Game_local.h"
+#ifdef MOD_BOTS
+#include "bots/BotAI.h"
+#endif
 
 const int NUM_RENDER_PORTAL_BITS	= idMath::BitsForInteger( PS_BLOCK_ALL );
 
@@ -323,6 +326,9 @@ void idGameLocal::Init( void ) {
 
 	// load default scripts
 	program.Startup( SCRIPT_DEFAULT );
+#ifdef MOD_BOTS
+	botAi::InitBotSystem();
+#endif
 
 	smokeParticles = new idSmokeParticles;
 
@@ -947,6 +953,10 @@ void idGameLocal::LoadMap( const char *mapName, int randseed ) {
 
 	// load navigation system for all the different monster sizes
 	for( i = 0; i < aasNames.Num(); i++ ) {
+#ifdef MOD_BOTS
+		// Elevator/teleporter entities must exist before bot reachability is built.
+		if (BOT_ENABLED() && aasNames[i] == BOT_AAS) continue;
+#endif
 		aasList[ i ]->Init( idStr( mapFileName ).SetFileExtension( aasNames[ i ] ).c_str(), mapFile->GetGeometryCRC() );
 	}
 
@@ -1202,6 +1212,15 @@ void idGameLocal::InitFromNewMap( const char *mapName, idRenderWorld *renderWorl
 
 	MapPopulate();
 
+#ifdef MOD_BOTS
+    if (BOT_ENABLED()) {
+        for (int i = 0; i < aasNames.Num(); ++i) {
+            if (aasNames[i] == BOT_AAS)
+                aasList[i]->Init(idStr(mapFileName).SetFileExtension(BOT_AAS), mapFile->GetGeometryCRC());
+        }
+    }
+#endif
+
 	mpGame.Reset();
 
 	mpGame.Precache();
@@ -2163,6 +2182,18 @@ void idGameLocal::SortActiveEntityList( void ) {
 		}
 	}
 
+#ifdef MOD_BOTS
+	if(BOT_ENABLED()) {
+		// TinMan: Make bots king of the think list, where they shall rule with an iron fist. Basically they should think before thier fakeclient ents
+		for ( ent = activeEntities.Next(); ent != NULL; ent = next_ent ) {
+			next_ent = ent->activeNode.Next();
+			if ( ent->IsType( botAi::Type ) ) {
+				ent->activeNode.Remove();
+				ent->activeNode.AddToFront( activeEntities );
+			}
+		}
+	}
+#endif
 	sortTeamMasters = false;
 	sortPushers = false;
 }
diff --git a/neo/game/Game_local.h b/neo/game/Game_local.h
index 1f72443..e82c289 100644
--- a/neo/game/Game_local.h
+++ b/neo/game/Game_local.h
@@ -70,6 +70,7 @@ extern idSoundWorld *				gameSoundWorld;
 class idEntity;
 class idActor;
 class idPlayer;
+class botAi;
 class idCamera;
 class idWorldspawn;
 class idTestModel;
@@ -322,6 +323,10 @@ public:
 	virtual void			HandleMainMenuCommands( const char *menuCommand, idUserInterface *gui );
 	virtual allowReply_t	ServerAllowClient( int numClients, const char *IP, const char *guid, const char *password, char reason[MAX_STRING_CHARS] );
 	virtual void			ServerClientConnect( int clientNum, const char *guid );
+#ifdef MOD_BOTS
+	void ServerBotClientBegin( int clientNum, const idDict *clientArgs );
+	friend class botAi;
+#endif
 	virtual void			ServerClientBegin( int clientNum );
 	virtual void			ServerClientDisconnect( int clientNum );
 	virtual void			ServerWriteInitialReliableMessages( int clientNum );
diff --git a/neo/game/Game_network.cpp b/neo/game/Game_network.cpp
index c26d906..6627d08 100644
--- a/neo/game/Game_network.cpp
+++ b/neo/game/Game_network.cpp
@@ -36,6 +36,9 @@ If you have questions concerning this license or the applicable additional terms
 #include "Player.h"
 
 #include "Game_local.h"
+#ifdef MOD_BOTS
+#include "bots/BotAI.h"
+#endif
 
 /*
 ===============================================================================
@@ -47,6 +50,82 @@ If you have questions concerning this license or the applicable additional terms
 ===============================================================================
 */
 
+#ifdef MOD_BOTS
+/*
+================
+idGameLocal::ServerBotClientBegin
+================
+*/
+void idGameLocal::ServerBotClientBegin(int clientNum, const idDict *clientArgs)
+{
+    idBitMsg	outMsg;
+    byte		msgBuf[MAX_GAME_MESSAGE_SIZE];
+
+    // initialize the decl remap
+    InitClientDeclRemap(clientNum);
+
+    // send message to initialize decl remap at the client (this is always the very first reliable game message)
+    outMsg.Init(msgBuf, sizeof(msgBuf));
+    outMsg.BeginWriting();
+    outMsg.WriteByte(GAME_RELIABLE_MESSAGE_INIT_DECL_REMAP);
+    networkSystem->ServerSendReliableMessage(clientNum, outMsg);
+
+    // spawn the player
+    // SpawnPlayer(clientNum);
+    {
+        idEntity	*ent;
+        idDict		args;
+
+        if(clientArgs)
+            args = *clientArgs;
+
+        // they can connect
+        Printf("SpawnBotPlayer: %i\n", clientNum);
+
+        args.SetBool("isBot", true);
+        args.SetInt("botID", clientNum);
+
+        args.SetInt("spawn_entnum", clientNum);
+        args.Set("name", va("player%d", clientNum + 1));
+        args.Set("classname", isMultiplayer ? "player_doommarine_mp" : "player_doommarine");
+
+        if (!SpawnEntityDef(args, &ent) || !entities[ clientNum ]) {
+            Error("Failed to spawn bot player as '%s'", args.GetString("classname"));
+        }
+
+        // make sure it's a compatible class
+        if (!ent->IsType(idPlayer::Type)) {
+            Error("'%s' spawned the bot player as a '%s'.  Bot Player spawnclass must be a subclass of idPlayer.", args.GetString("classname"), ent->GetClassname());
+        }
+
+        if (clientNum >= numClients) {
+            numClients = clientNum + 1;
+        }
+
+        mpGame.SpawnPlayer(clientNum);
+        // Spawn() establishes the hidden/spectator invariants first. Creating
+        // the brain from Player::Init reenters Spectate halfway through Spawn.
+        static_cast<idPlayer *>(ent)->SpawnBot();
+    }
+
+    if (clientNum == localClientNum) {
+        mpGame.EnterGame(clientNum);
+    }
+
+    // send message to spawn the player at the clients
+    outMsg.Init(msgBuf, sizeof(msgBuf));
+    outMsg.BeginWriting();
+    outMsg.WriteByte(GAME_RELIABLE_MESSAGE_SPAWN_PLAYER);
+    outMsg.WriteByte(clientNum);
+    outMsg.WriteInt(spawnIds[ clientNum ]);
+    networkSystem->ServerSendReliableMessage(-1, outMsg);
+    if(BOT_ENABLED()) {
+        // TinMan: Tell engine to spit out bots userinfo to clients
+        botAi::UpdateUI();
+    }
+}
+#endif
+
 // adds tags to the network protocol to detect when things go bad ( internal consistency )
 // NOTE: this changes the network protocol
 #ifndef ASYNC_WRITE_TAGS
@@ -335,6 +414,11 @@ void idGameLocal::ServerClientBegin( int clientNum ) {
 	outMsg.WriteByte( clientNum );
 	outMsg.WriteInt( spawnIds[ clientNum ] );
 	networkSystem->ServerSendReliableMessage( -1, outMsg );
+#ifdef MOD_BOTS
+	// Fake game clients are not in AsyncServer's connected-client iteration.
+	// Send their userinfo to humans who arrive after the bots were spawned.
+	if (BOT_ENABLED()) botAi::UpdateUI();
+#endif
 }
 
 /*
@@ -596,6 +680,9 @@ void idGameLocal::ServerWriteSnapshot( int clientNum, int sequence, idBitMsg &ms
 #endif
 
 	// create the snapshot
+#ifdef MOD_BOTS
+	if (isMultiplayer && botAi::IsAvailable()) botAi::WriteUserCmdsToSnapshot(msg);
+#endif
 	for( ent = spawnedEntities.Next(); ent != NULL; ent = ent->spawnNode.Next() ) {
 
 		// if the entity is not in the player PVS
@@ -1015,6 +1102,9 @@ void idGameLocal::ClientReadSnapshot( int clientNum, int sequence, const int gam
 #endif
 
 	// read all entities from the snapshot
+#ifdef MOD_BOTS
+	if (isMultiplayer && botAi::IsAvailable()) botAi::ReadUserCmdsFromSnapshot(msg);
+#endif
 	for ( i = msg.ReadBits( GENTITYNUM_BITS ); i != ENTITYNUM_NONE; i = msg.ReadBits( GENTITYNUM_BITS ) ) {
 
 		base = clientEntityStates[clientNum][i];
diff --git a/neo/game/Mover.h b/neo/game/Mover.h
index 7b95598..99e0ae5 100644
--- a/neo/game/Mover.h
+++ b/neo/game/Mover.h
@@ -318,6 +318,11 @@ public:
 
 	void					SetPortalState( bool open );
 
+#ifdef MOD_BOTS
+	const idVec3 &GetPosition1() const { return pos1; }
+	const idVec3 &GetPosition2() const { return pos2; }
+#endif
+
 protected:
 	idVec3					pos1;
 	idVec3					pos2;
diff --git a/neo/game/MultiplayerGame.h b/neo/game/MultiplayerGame.h
index 15fc2eb..445a68d 100644
--- a/neo/game/MultiplayerGame.h
+++ b/neo/game/MultiplayerGame.h
@@ -100,6 +100,9 @@ typedef enum {
 
 class idMultiplayerGame {
 public:
+#ifdef MOD_BOTS
+	int GetBotProbeFrags(int clientNum) const { return playerState[clientNum].fragCount; }
+#endif
 
 					idMultiplayerGame();
 
diff --git a/neo/game/Player.cpp b/neo/game/Player.cpp
index 8caaab4..8ea635d 100644
--- a/neo/game/Player.cpp
+++ b/neo/game/Player.cpp
@@ -40,6 +40,19 @@ If you have questions concerning this license or the applicable additional terms
 #include "Camera.h"
 #include "Fx.h"
 #include "Misc.h"
+#ifdef MOD_BOTS
+#include "bots/BotAI.h"
+#endif
+
+#ifdef MOD_BOTS
+void idPlayer::SetupBot(botAi *brain) { bot = brain; }
+bool idPlayer::IsBotAvailable() const { return IsBot() && bot.GetEntity() != NULL; }
+botAi *idPlayer::GetBot() { return bot.GetEntity(); }
+void idPlayer::SpawnBot() {
+    if (!BOT_ENABLED() || !IsBot() || !botAi::PlayerHasBotSlot(entityNumber) || bot.GetEntity()) return;
+    bot = botAi::SpawnBot(this);
+}
+#endif
 
 const int ASYNC_PLAYER_INV_AMMO_BITS = idMath::BitsForInteger( 999 );	// 9 bits to cover the range [0, 999]
 const int ASYNC_PLAYER_INV_CLIP_BITS = -7;								// -7 bits to cover the range [-1, 60]
@@ -953,6 +966,9 @@ idPlayer::idPlayer
 ==============
 */
 idPlayer::idPlayer() {
+#ifdef MOD_BOTS
+	bot = NULL;
+#endif
 	memset( &usercmd, 0, sizeof( usercmd ) );
 
 	noclip					= false;
@@ -1609,6 +1625,11 @@ Release any resources used by the player.
 ==============
 */
 idPlayer::~idPlayer() {
+#ifdef MOD_BOTS
+	if (IsBot()) botAi::ReleaseBotSlot(entityNumber);
+	delete bot.GetEntity();
+	bot = NULL;
+#endif
 	delete weapon.GetEntity();
 	weapon = NULL;
 }
@@ -2085,6 +2106,9 @@ idPlayer::PrepareForRestart
 ================
 */
 void idPlayer::PrepareForRestart( void ) {
+#ifdef MOD_BOTS
+	if (bot.GetEntity()) bot.GetEntity()->PrepareForRestart();
+#endif
 	ClearPowerUps();
 	Spectate( true );
 	forceRespawn = true;
@@ -2115,6 +2139,10 @@ void idPlayer::Restart( void ) {
 
 	useInitialSpawns = true;
 	UpdateSkinSetup( true );
+#ifdef MOD_BOTS
+	if (bot.GetEntity()) bot.GetEntity()->Restart();
+	else SpawnBot(); // LocalMapRestart deletes non-player AI entities.
+#endif
 }
 
 /*
diff --git a/neo/game/Player.h b/neo/game/Player.h
index b411a0c..e8a56fb 100644
--- a/neo/game/Player.h
+++ b/neo/game/Player.h
@@ -40,6 +40,7 @@ If you have questions concerning this license or the applicable additional terms
 #include "GameEdit.h"
 
 class idAI;
+class botAi;
 
 /*
 ===============================================================================
@@ -207,6 +208,16 @@ typedef struct {
 } aasLocation_t;
 
 class idPlayer : public idActor {
+#ifdef MOD_BOTS
+	friend class botAi;
+	idEntityPtr<botAi> bot;
+public:
+	void SpawnBot();
+	void SetupBot(botAi *brain);
+	bool IsBot() const { return spawnArgs.GetBool("isBot"); }
+	bool IsBotAvailable() const;
+	botAi *GetBot();
+#endif
 public:
 	enum {
 		EVENT_IMPULSE = idEntity::EVENT_MAXEVENTS,
diff --git a/neo/game/Weapon.cpp b/neo/game/Weapon.cpp
index f21a2fe..6134e88 100644
--- a/neo/game/Weapon.cpp
+++ b/neo/game/Weapon.cpp
@@ -1877,6 +1877,14 @@ idWeapon::PresentWeapon
 void idWeapon::PresentWeapon( bool showViewModel ) {
 	playerViewOrigin = owner->firstPersonViewOrigin;
 	playerViewAxis = owner->firstPersonViewAxis;
+#ifdef MOD_BOTS
+	// Dedicated rendering has no world-model joints: keep the fallback muzzle
+	// at the player's eyes instead of the unrendered model's stale origin.
+	idEntity *botWorldModel = worldModel.GetEntity();
+	if (gameLocal.isMultiplayer && botWorldModel && !botWorldModel->GetRenderEntity()->hModel) {
+		botWorldModel->GetPhysics()->SetOrigin(owner->GetEyePosition());
+	}
+#endif
 
 	// calculate weapon position based on player movement bobbing
 	owner->CalculateViewWeaponPos( viewWeaponOrigin, viewWeaponAxis );
diff --git a/neo/game/ai/AAS.cpp b/neo/game/ai/AAS.cpp
index 10ab9c2..4de9972 100644
--- a/neo/game/ai/AAS.cpp
+++ b/neo/game/ai/AAS.cpp
@@ -31,6 +31,10 @@ If you have questions concerning this license or the applicable additional terms
 #include "tools/compilers/aas/AASFileManager.h"
 
 #include "ai/AAS_local.h"
+#ifdef MOD_BOTS
+#include "bots/BotAI.h"
+#include "bots/BotAASBuild.h"
+#endif
 
 /*
 ============
@@ -56,6 +60,9 @@ idAASLocal::idAASLocal
 */
 idAASLocal::idAASLocal( void ) {
 	file = NULL;
+#ifdef MOD_BOTS
+	botAASBuilder = NULL;
+#endif
 }
 
 /*
@@ -65,6 +72,9 @@ idAASLocal::~idAASLocal
 */
 idAASLocal::~idAASLocal( void ) {
 	Shutdown();
+#ifdef MOD_BOTS
+	delete botAASBuilder;
+#endif
 }
 
 /*
@@ -85,6 +95,14 @@ bool idAASLocal::Init( const idStr &mapName, unsigned int mapFileCRC ) {
 			common->DWarning( "Couldn't load AAS file: '%s'", mapName.c_str() );
 			return false;
 		}
+#ifdef MOD_BOTS
+		// Only the dedicated bot navigation mesh gets dynamic links.
+		if (BOT_ENABLED() && idStr::CheckExtension(mapName, BOT_AAS)) {
+			if (!botAASBuilder) botAASBuilder = new BotAASBuild;
+			botAASBuilder->Init(this);
+			botAASBuilder->AddReachabilities();
+		}
+#endif
 		SetupRouting();
 	}
 	return true;
@@ -97,6 +115,9 @@ idAASLocal::Shutdown
 */
 void idAASLocal::Shutdown( void ) {
 	if ( file ) {
+#ifdef MOD_BOTS
+		if (botAASBuilder) botAASBuilder->FreeAAS();
+#endif
 		ShutdownRouting();
 		RemoveAllObstacles();
 		AASFileManager->FreeAAS( file );
diff --git a/neo/game/ai/AAS.h b/neo/game/ai/AAS.h
index 08ab9c0..2e57e57 100644
--- a/neo/game/ai/AAS.h
+++ b/neo/game/ai/AAS.h
@@ -43,7 +43,8 @@ enum {
 	PATHTYPE_WALK,
 	PATHTYPE_WALKOFFLEDGE,
 	PATHTYPE_BARRIERJUMP,
-	PATHTYPE_JUMP
+	PATHTYPE_JUMP,
+	PATHTYPE_ELEVATOR
 };
 
 typedef struct aasPath_s {
diff --git a/neo/game/ai/AAS_local.h b/neo/game/ai/AAS_local.h
index 6d32c2e..a9fc23f 100644
--- a/neo/game/ai/AAS_local.h
+++ b/neo/game/ai/AAS_local.h
@@ -83,6 +83,11 @@ private:
 
 
 class idAASLocal : public idAAS {
+#ifdef MOD_BOTS
+	friend class BotAASBuild;
+	friend class botAi;
+	class BotAASBuild *botAASBuilder;
+#endif
 public:
 								idAASLocal( void );
 	virtual						~idAASLocal( void );
diff --git a/neo/game/ai/AAS_pathing.cpp b/neo/game/ai/AAS_pathing.cpp
index 916b5b6..b70f847 100644
--- a/neo/game/ai/AAS_pathing.cpp
+++ b/neo/game/ai/AAS_pathing.cpp
@@ -384,6 +384,22 @@ bool idAASLocal::WalkPathToGoal( aasPath_t &path, int areaNum, const idVec3 &ori
 			path.secondaryGoal = reach->end;
 			path.reachability = reach;
 			break;
+#ifdef MOD_BOTS
+		case TFL_ELEVATOR: {
+			int time;
+			idReachability *next = NULL;
+			path.secondaryGoal = reach->end;
+			RouteToGoalArea(reach->toAreaNum, reach->end, goalAreaNum, travelFlags, time, &next);
+			// A corrupt/cyclic elevator route must not hang the server.
+			for (int hops = 0; next && next->travelType == TFL_ELEVATOR && hops < file->GetNumAreas(); ++hops) {
+				path.secondaryGoal = next->end;
+				RouteToGoalArea(next->toAreaNum, next->end, goalAreaNum, travelFlags, time, &next);
+			}
+			path.type |= PATHTYPE_ELEVATOR;
+			path.reachability = reach;
+			break;
+		}
+#endif
 		default:
 			break;
 	}
diff --git a/neo/game/ai/AAS_routing.cpp b/neo/game/ai/AAS_routing.cpp
index eafd01e..d224663 100644
--- a/neo/game/ai/AAS_routing.cpp
+++ b/neo/game/ai/AAS_routing.cpp
@@ -689,7 +689,7 @@ idReachability *idAASLocal::GetAreaReachability( int areaNum, int reachabilityNu
 idAASLocal::ClusterAreaNum
 ============
 */
-ID_INLINE int idAASLocal::ClusterAreaNum( int clusterNum, int areaNum ) const {
+int idAASLocal::ClusterAreaNum( int clusterNum, int areaNum ) const {
 	int side, areaCluster;
 
 	areaCluster = file->GetArea( areaNum ).cluster;
diff --git a/neo/game/gamesys/SysCmds.cpp b/neo/game/gamesys/SysCmds.cpp
index 732d506..9b44a7d 100644
--- a/neo/game/gamesys/SysCmds.cpp
+++ b/neo/game/gamesys/SysCmds.cpp
@@ -43,6 +43,9 @@ If you have questions concerning this license or the applicable additional terms
 #include "Misc.h"
 
 #include "SysCmds.h"
+#ifdef MOD_BOTS
+#include "bots/BotAI.h"
+#endif
 
 /*
 ==================
@@ -2304,6 +2307,21 @@ so it can perform tab completion
 =================
 */
 void idGameLocal::InitConsoleCommands( void ) {
+#ifdef MOD_BOTS // TinMan: Bot console commmands
+    cmdSystem->AddCommand( "addBot",				botAi::Addbot_f,			CMD_FL_GAME,				"adds a new bot", botAi::ArgCompletion_addBot );
+    cmdSystem->AddCommand( "removeBot",				botAi::Removebot_f,			CMD_FL_GAME,				"removes bot specified by id (1,31)", botAi::ArgCompletion_botSlots /*, idCmdSystem::ArgCompletion_Integer<0,( botAi::BOT_MAX_BOTS - 1 )>*/ );
+
+    cmdSystem->AddCommand("addBots", botAi::Cmd_AddBots_f, CMD_FL_GAME, "add multiplayer bots batch", botAi::ArgCompletion_addBot);
+    cmdSystem->AddCommand("removeBots", botAi::Cmd_RemoveBots_f, CMD_FL_GAME, "disconnect multi bots by client ID", botAi::ArgCompletion_botSlots);
+    cmdSystem->AddCommand("fillBots", botAi::Cmd_FillBots_f, CMD_FL_GAME, "fill bots to maximum of server");
+    cmdSystem->AddCommand("appendBots", botAi::Cmd_AppendBots_f, CMD_FL_GAME, "append more bots(over maximum of server)");
+    cmdSystem->AddCommand("cleanBots", botAi::Cmd_CleanBots_f, CMD_FL_GAME, "disconnect all bots");
+    cmdSystem->AddCommand("truncBots", botAi::Cmd_TruncBots_f, CMD_FL_GAME, "disconnect last bots");
+    cmdSystem->AddCommand("sabot", botAi::Cmd_BotInfo_f, CMD_FL_GAME, "debug SaBot info");
+    cmdSystem->AddCommand("botLevel", botAi::Cmd_SetupBotLevel_f, CMD_FL_GAME, "setup all bot level", botAi::ArgCompletion_botLevel);
+    cmdSystem->AddCommand("botWeapons", botAi::Cmd_SetupBotWeapons_f, CMD_FL_GAME, "setup all bot initial weapons", botAi::ArgCompletion_botWeapons);
+    cmdSystem->AddCommand("botAmmo", botAi::Cmd_SetupBotAmmo_f, CMD_FL_GAME, "setup all bot initial weapons ammo");
+#endif
 	cmdSystem->AddCommand( "listTypeInfo",			ListTypeInfo_f,				CMD_FL_GAME,				"list type info" );
 	cmdSystem->AddCommand( "writeGameState",		WriteGameState_f,			CMD_FL_GAME,				"write game state" );
 	cmdSystem->AddCommand( "testSaveGame",			TestSaveGame_f,				CMD_FL_GAME|CMD_FL_CHEAT,	"test a save game for a level" );
diff --git a/neo/tools/compilers/aas/AASFile.h b/neo/tools/compilers/aas/AASFile.h
index 9ae001c..4b28644 100644
--- a/neo/tools/compilers/aas/AASFile.h
+++ b/neo/tools/compilers/aas/AASFile.h
@@ -284,6 +284,7 @@ private:
 
 
 class idAASFile {
+	friend class BotAASBuild;
 public:
 	virtual						~idAASFile( void ) {}
 
diff --git a/neo/game/bots/BotAASBuild.cpp b/neo/game/bots/BotAASBuild.cpp
index e55ea81..12a837c 100644
--- a/neo/game/bots/BotAASBuild.cpp
+++ b/neo/game/bots/BotAASBuild.cpp
@@ -1,9 +1,14 @@
-#include "../../idlib/precompiled.h"
+#include "sys/platform.h"
+#include "renderer/RenderWorld.h"
 #pragma hdrstop
 
 #ifdef MOD_BOTS // cusTom3 
 
 #include "../Game_local.h"
+#include "BotAI.h"
+#include "../Mover.h"
+#include "../Misc.h"
+#include "../Trigger.h"
 #include "BotAASBuild.h"
 
 idCVar aas_showElevators( "aas_showElevators", "0", CVAR_SYSTEM | CVAR_BOOL | CVAR_NOCHEAT, "show elevator debug stuff" );
@@ -35,22 +40,12 @@ BotAASBuild::AddReachabilities
 void BotAASBuild::AddReachabilities( void )
 {
 
-    // steal the portals list so i can manipulate it
-    originalPortals.list = file->portals.list;
-    originalPortals.granularity = file->portals.granularity;
-    originalPortals.num = file->portals.num;
-    originalPortals.size = file->portals.size;
-
-    portals.Append(file->portals);
-    file->portals.list = portals.list;
-
-    originalPortalIndex.list = file->portalIndex.list;
-    originalPortalIndex.granularity = file->portalIndex.granularity;
-    originalPortalIndex.num = file->portalIndex.num;
-    originalPortalIndex.size = file->portalIndex.size;
-
-    portalIndex.Append(file->portalIndex);
-    file->portalIndex.list = portalIndex.list;
+    // Transfer ownership, then make independent mutable lists. The reference
+    // implementation aliased storage but retained the old list capacity.
+    originalPortals.Swap(file->portals);
+    file->portals = originalPortals;
+    originalPortalIndex.Swap(file->portalIndex);
+    file->portalIndex = originalPortalIndex;
 
     AddElevatorReachabilities();
     AddTransporterReachabilities();
@@ -149,6 +144,8 @@ void BotAASBuild::AddElevatorReachabilities( void )
 
             // check for portals going up the plat before processing
             idVec3 firstPortal, lastPortal;
+            firstPortal.Zero();
+            lastPortal.Zero();
             int firstPortalNum, lastPortalNum;
             firstPortalNum = lastPortalNum = 0;
             bool reachabilityCreated = false;
@@ -582,47 +579,11 @@ cusTom3 - this crashes on game/mp/d3dm1???? but not on modified (clipped) versio
 */
 void BotAASBuild::FreeAAS()
 {
-    //OutputAASReachInfo( file );
-
-    // remove the references that point to engine objects - as innefficiently as possible ;)
-    for ( int i = 0; i < originalPortals.Num(); i++ )
-    {
-        // remove the first one from the list, it will move the rest (go backwards at least lazy man)
-        file->portals.RemoveIndex( 0 );
-    }
-
-    // if file->portals was resized, portals points to a place that has been deleted.
-    portals.list = file->portals.list;
-    portals.num = file->portals.num;
-    portals.size = file->portals.size;
-    portals.granularity = file->portals.granularity;
-
-    // reset the aas portals list back to the orignal
-    file->portals.list = originalPortals.list;
-    file->portals.size = originalPortals.size;
-    file->portals.num= originalPortals.num;
-    file->portals.granularity = originalPortals.granularity;
-
-    for ( int i = 0; i < originalPortalIndex.Num(); i++ )
-    {
-        // remove the first one from the list, it will move the rest (go backwards at least lazy man)
-        file->portalIndex.RemoveIndex( 0 );
-    }
-
-    // if file->portalIndex was resized, portalIndex points to a place that has been deleted.
-    portalIndex.list = file->portalIndex.list;
-    portalIndex.num = file->portalIndex.num;
-    portalIndex.size = file->portalIndex.size;
-    portalIndex.granularity = file->portalIndex.granularity;
-
-    // reset the aas portalIndex list back to the orignal
-    file->portalIndex.list = originalPortalIndex.list;
-    file->portalIndex.size = originalPortalIndex.size;
-    file->portalIndex.num= originalPortalIndex.num;
-    file->portalIndex.granularity = originalPortalIndex.granularity;
-
-    portals.Clear();
-    portalIndex.Clear();
+    if (!file) return;
+    file->portals.Swap(originalPortals);
+    originalPortals.Clear();
+    file->portalIndex.Swap(originalPortalIndex);
+    originalPortalIndex.Clear();
 
     // for each reachability added unmanipulate aas file
     int numReach = reachabilities.Num();
@@ -651,7 +612,7 @@ void BotAASBuild::FreeAAS()
         if ( r == file->areas[r->toAreaNum].rev_reach )
         {
             // only one in list, tell area
-            file->areas[r->toAreaNum].rev_reach = NULL;
+            file->areas[r->toAreaNum].rev_reach = r->rev_next;
         }
         else
         {
diff --git a/neo/game/bots/BotAASBuild.h b/neo/game/bots/BotAASBuild.h
index 695a69a..a4f4dac 100644
--- a/neo/game/bots/BotAASBuild.h
+++ b/neo/game/bots/BotAASBuild.h
@@ -55,24 +55,13 @@ private:
 
 ID_INLINE BotAASBuild::BotAASBuild( void )
 {
+    aas = NULL;
+    file = NULL;
     reachabilities.Resize( 128, 128 );
 }
 
 ID_INLINE BotAASBuild::~BotAASBuild( void )
 {
-    // clear out the original so it doesn't try and delete the server list ;)
-    originalPortals.list = NULL;
-    originalPortals.size = 0;
-    originalPortals.num = 0;
-
-    originalPortalIndex.list = NULL;
-    originalPortalIndex.size = 0;
-    originalPortalIndex.num = 0;
-
-    // if these are not cleared correctly in FreeAAS this will error, dont like this, but hey, whatever
-    portals.Clear();
-    portalIndex.Clear();
-
     reachabilities.DeleteContents( true );
 }
 
@@ -80,4 +69,4 @@ ID_INLINE const idBounds & BotAASBuild::DefaultBotBounds( void )
 {
     return file->GetSettings().boundingBoxes[0];
 }
-#endif /* !__BOTAASBUILD_H__ */
\ No newline at end of file
+#endif /* !__BOTAASBUILD_H__ */
diff --git a/neo/game/bots/BotAI.cpp b/neo/game/bots/BotAI.cpp
index ddfadf3..a236e48 100644
--- a/neo/game/bots/BotAI.cpp
+++ b/neo/game/bots/BotAI.cpp
@@ -1,10 +1,20 @@
 
-#include "../../idlib/precompiled.h"
+#include "sys/platform.h"
+#include "framework/FileSystem.h"
+#include "renderer/RenderWorld.h"
+#include "idlib/MapFile.h"
 #pragma hdrstop
 
 #ifdef MOD_BOTS
 
 #include "../Game_local.h"
+#include "BotAI.h"
+#include "../Mover.h"
+#include "../Moveable.h"
+#include "../Misc.h"
+#include "../gamesys/SysCvar.h"
+#include "../../framework/DeclEntityDef.h"
+#include "../../framework/async/NetworkSystem.h"
 
 #ifdef _MOD_FULL_BODY_AWARENESS
 #define Player_GetViewPos(p, o, a) GetPlayerViewPos(p, o, a)
@@ -544,9 +554,20 @@ void botAi::ReadUserCmdsFromSnapshot( const idBitMsg &msg )
     int numBots;
 
     numBots = msg.ReadBits( 5 );
+    // Five index bits plus 112 command bits per bot. Fail before any array access.
+    if (numBots < 0 || msg.GetRemainingReadBits() < numBots * 117) {
+        gameLocal.Error("Truncated SABot user commands");
+        return;
+    }
+    unsigned int seen = 0;
     for ( i = 0; i < numBots; i++ )
     {
         int clientNum = msg.ReadBits( 5 );
+        if (clientNum < BOT_START_INDEX || clientNum >= MAX_CLIENTS || (seen & (1u << clientNum))) {
+            gameLocal.Error("Invalid SABot client slot");
+            return;
+        }
+        seen |= 1u << clientNum;
 
         usercmd_t &cmd = gameLocal.usercmds[clientNum]; // TinMan: Overwrite the usrcmds, as bot isn't propper client this will be null anyway.
         cmd.buttons     = msg.ReadByte();
diff --git a/neo/game/bots/BotAI.h b/neo/game/bots/BotAI.h
index b0bb262..2dbf43c 100644
--- a/neo/game/bots/BotAI.h
+++ b/neo/game/bots/BotAI.h
@@ -1,5 +1,10 @@
 #ifndef __BOTAI_H__
 #define __BOTAI_H__
+
+#include "../ai/AI.h"
+#include "../Player.h"
+#include "../script/Script_Thread.h"
+#include "../ai/AAS_local.h"
 /*
 ===============================================================================
 
diff --git a/neo/game/bots/BotAI_cfg.cpp b/neo/game/bots/BotAI_cfg.cpp
index f8e7cb9..d7b98b5 100644
--- a/neo/game/bots/BotAI_cfg.cpp
+++ b/neo/game/bots/BotAI_cfg.cpp
@@ -436,10 +436,17 @@ int botAi::MakeWeaponMask(const char *wp)
     if(!idStr::Cmp("*", wp))
         return BOT_ALL_MP_WEAPON;
     idStrList list;
-    if(idStr::SplitUnique(list, wp, ',') == 0)
-        return 0;
-    for(int i = 0; i < list.Num(); i++)
-        idStr::StripWhitespace(list[i]);
+    if (!wp) return 0;
+    const char *start = wp;
+    for (const char *p = wp; ; ++p) {
+        if (*p != ',' && *p != '\0') continue;
+        const char *end = p;
+        while (start < end && (unsigned char)*start <= ' ') ++start;
+        while (end > start && (unsigned char)end[-1] <= ' ') --end;
+        if (end > start) list.AddUnique(idStr(start, 0, end - start));
+        if (!*p) break;
+        start = p + 1;
+    }
     return MakeWeaponMask(list);
 }
 
@@ -551,4 +558,3 @@ void botAi::InsertEmptyAmmo(idDict &dict)
 
 #undef BOT_MW
 }
-
diff --git a/neo/game/bots/BotAI_cmd.cpp b/neo/game/bots/BotAI_cmd.cpp
index 849855c..ebf62a3 100644
--- a/neo/game/bots/BotAI_cmd.cpp
+++ b/neo/game/bots/BotAI_cmd.cpp
@@ -459,6 +459,22 @@ int botAi::CheckRestClients(int num)
 
 void botAi::Cmd_BotInfo_f(const idCmdArgs& args)
 {
+    // Read-only native-runtime evidence; no scripted movement or combat.
+    int liveBrains = 0;
+    for (int i = 0; i < MAX_CLIENTS; ++i) {
+        idPlayer *player = FindBotClient(i);
+        if (player && player->IsBotAvailable()) ++liveBrains;
+    }
+    gameLocal.Printf("SABOT_FRAME {\"time\":%d,\"bots\":%d}\n", gameLocal.time, liveBrains);
+    for (int i = 0; i < MAX_CLIENTS; ++i) {
+        idPlayer *player = FindBotClient(i);
+        if (!player || !player->IsBotAvailable()) continue;
+        const idVec3 &origin = player->GetPhysics()->GetOrigin();
+        const usercmd_t &cmd = gameLocal.usercmds[i];
+        gameLocal.Printf("SABOT_SAMPLE {\"time\":%d,\"slot\":%d,\"health\":%d,\"spectating\":%d,\"origin\":[%.3f,%.3f,%.3f],\"buttons\":%d,\"move\":[%d,%d,%d],\"weapon\":%d,\"frags\":%d}\n",
+            gameLocal.time, i, player->health, player->spectating, origin.x, origin.y, origin.z,
+            cmd.buttons, cmd.forwardmove, cmd.rightmove, cmd.upmove, player->currentWeapon, gameLocal.mpGame.GetBotProbeFrags(i));
+    }
     gameLocal.Printf("SABot(a7)\n");
     gameLocal.Printf("game is multiplayer: %d\n", gameLocal.isMultiplayer);
     gameLocal.Printf("game is server: %d\n", gameLocal.isServer);
@@ -892,4 +908,3 @@ void botAi::ArgCompletion_botWeapons( const idCmdArgs &args, void(*callback)( co
 
 #undef BOT_MW
 }
-
diff --git a/neo/game/bots/BotAI_manager.cpp b/neo/game/bots/BotAI_manager.cpp
index 3390cd0..29dcde5 100644
--- a/neo/game/bots/BotAI_manager.cpp
+++ b/neo/game/bots/BotAI_manager.cpp
@@ -1,29 +1,12 @@
 
 int botAi::FindIdleBotSlot(void)
 {
-    int i;
-
-    for ( i = BOT_START_INDEX; i < BOT_MAX_NUM; i++ )
-    {
-        if ( bots[i].inUse )
-        {
-            // TinMan: make sure it isn't an orphaned spot
-            if ( gameLocal.entities[ bots[i].clientID ] && gameLocal.entities[ bots[i].entityNum ]  )
-            {
-                continue;
-            }
-            else
-            {
-                break;
-            }
-        }
-        else
-        {
-            break;
-        }
+    // Prefer high slots, leaving ordinary engine connections at the low end.
+    // Never reclaim a live player, including a human whose slot is not in bots[].
+    for (int i = BOT_MAX_NUM - 1; i >= BOT_START_INDEX; --i) {
+        if (!bots[i].inUse && gameLocal.entities[i] == NULL) return i;
     }
-
-    return i;
+    return BOT_MAX_NUM;
 }
 
 void botAi::UpdateUI(void)
@@ -84,6 +67,7 @@ bool botAi::GenerateAAS(void)
 
 idPlayer * botAi::FindBotClient(int clientID)
 {
+    if (clientID < 0 || clientID >= MAX_CLIENTS) return NULL;
     idEntity *entity = gameLocal.entities[ clientID ];
-    return (entity->IsType(idPlayer::Type) ? static_cast<idPlayer *>(entity) : NULL);
+    return (entity && entity->IsType(idPlayer::Type) ? static_cast<idPlayer *>(entity) : NULL);
 }
diff --git a/neo/game/bots/BotSabot.cpp b/neo/game/bots/BotSabot.cpp
index 6deec59..c54e7dc 100644
--- a/neo/game/bots/BotSabot.cpp
+++ b/neo/game/bots/BotSabot.cpp
@@ -1,9 +1,10 @@
-#include "../../idlib/precompiled.h"
+#include "sys/platform.h"
 #pragma hdrstop
 
 #ifdef MOD_BOTS
 
 #include "../Game_local.h"
+#include "BotAI.h"
 
 /*
 ===============================================================================
@@ -99,4 +100,4 @@ void botSabot::Think( void )
     }
 }
 
-#endif
\ No newline at end of file
+#endif
