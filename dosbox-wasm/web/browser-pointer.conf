# SimCity uses scaled absolute coordinates; NFS's adapter owns relative capture.
# Disable DOSBox's separate desktop auto-lock so it cannot swallow a browser
# gesture or switch mouse modes independently of the page's capture state.
[sdl]
autolock=false
sensitivity=100
