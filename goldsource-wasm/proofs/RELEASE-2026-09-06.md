# GoldSource cumulative live release — 2026-09-06

Later the same day, the [CS menu-only release](CS-MENU-GLOBALS-2026-09-06.md)
superseded this image and fixed the Resume/background/Console failures recorded
below. This remains the historical cumulative-release and data-migration record;
its accepted data installation is still active. Use the newer release audit for
current installed-image checks, not this checkpoint's frozen image assertions.

The exact 32932 candidate is now **installed on 8017**. This releases the
stored-WAD loading repair, native save-key hints, explicit-server fallback
guard, and pending-capture/loading-canvas framework repairs. The prior
log-selection repair is retained. It is not sustained capture, audible
playback, full-campaign or Counter-Strike model-overflow acceptance.

## Package and deployment

- Canonical image: `goldsource-wasm:dev`,
  `967561eca15081284748f7a46178a1f5d8f8024a3aa182e4a95e2a406ca9c2ce`.
- Container: `4e0f9e154e4de5b679a4267692a8ef09829521c88a90b246ad64515ce3ad6d3d`.
- Started: `2026-09-06T08:55:19.416789299Z`, restart count zero.
- Framework remains 0.9.6, owned checkout pin
  `ebb1ebe35ad8224a9080279a6529414db42d3284` plus the tested local repairs;
  this is not a claim of a newly published immutable framework release.

The [pre-release record](goldsource-release-before-2026-09-06.json) and
[installed audit](goldsource-release-installed-2026-09-06.json) compare 38
immutable package files. The generic menu library is replaced at its new
content-addressed path. Four same-named files also differ: browser adapter,
data manifest, and the framework JS in its two image locations. The other
33 files—including engine, filesystem, renderers, game modules, CS menu,
bootstrap/logger, CSS, artwork and HTTP server—are unchanged. All 27 tested
HTTP files match the accepted hashes; all four owner-data readiness checks
pass, and direct private-data/report routes remain 404.

Full GoldSource tests pass before and after updating the source manifest and
migration helper: `/tmp/goldsource-release-suite-final-20260906.log`.
Five packager/stager fixtures include explicit-original-manifest migration
and rejection of mismatched old data; all ten compiled menu-lookup cases pass.
Earlier save/WAD evidence checks pass again. Their detailed causes and
negative controls remain in [the proof index](README.md).

Only `docker compose ... up -d --no-deps goldsource` was invoked. The lab
image contract now pins this exact ID. Image audits pass before and after
the swap. All 94 other containers retain exact IDs, images, start times,
restart counts, state and mounts. Counter-Strike's native host was neither
restarted nor changed. Both RTCW services, OpenRCT2, Quake III and SimCity
retain their previously accepted live deployments.

## Private data preservation

Live RW bind:

`/home/ted/wasm-game-data/goldsource-stored-wad-20260906/data` → `/data`.

The original `/home/ted/wasm-game-data/goldsource` directory remains untouched,
including all 16 original files. The existing stager rebuilt separate copies
from those originals and re-read every one of 10,174 archive members, checking
uncompressed payload identity and identical non-WAD compressed streams.
Thirty-nine WADs are stored instead of deflated; the four archives grow by
43,854,721 bytes in total. Twelve descriptors/icons remain byte-identical.
New archive hashes and payload-index hashes exactly match the earlier accepted
candidate; no content or native game binaries were substituted.

The replacement root has mode 0700. Data and the staging report stay outside
the repository and image. The audit rejects undeclared extra original files,
so a later owner's additions cannot be silently dropped by this migration.
No original files were deleted or overwritten.

The source `web/wasm-game-data.json` now matches the accepted replacement
manifest exactly. The [original manifest](goldsource-release-original-data-manifest-2026-09-06.json)
is retained as metadata only. The stager accepts `--source-manifest` so an old
installation can be migrated alongside current browser files without reverting
the source site or weakening hash validation:

```sh
python3 goldsource-wasm/scripts/stage-stored-wad-candidate.py \
  /home/ted/wasm-game-data/goldsource \
  /home/ted/Development/wasm-games/goldsource-wasm/web \
  /path/to/new-empty-private-output \
  --source-manifest goldsource-wasm/proofs/goldsource-release-original-data-manifest-2026-09-06.json
```

Do not pair the new manifest/image with the old archives. A future replacement
must preserve any new imports in the active directory, not blindly re-stage
only the historical installation.

## Chrome compatibility-save baseline

The old live Blue Shift Load Game menu contains
[no existing saves](goldsource-release-old-blue-shift-save-list-2026-09-06.jpg).
Normal New Game → Medium reaches the textured tram with 100 health; native
startup again takes about 20 seconds from `Host_EndGame` at 04:49:06 local
time to `Game started` at 04:49:26 (resources finish 04:49:28).
No fixed frame/time-scale or direct map command was used.

The old [Save/load menu](goldsource-release-old-blue-shift-save-actions-2026-09-06.jpg)
reproduces both `<KEY NOT FOUND>` hints. Its New saved game slot writes
`save/save000.sav` at 04:53:06. The
[native save record](goldsource-release-old-blue-shift-saved-2026-09-06.json)
and screenshot are retained; no existing slot was overwritten. A read-only
native `status` query confirms `ba_tram1`, then native `disconnect` completes
normally at 04:54:55 before the tab is cleared and the service is swapped.

## Installed Chrome checks

On the same live origin and Blue Shift persistence namespace, the new engine's
[Load Game list](goldsource-release-installed-blue-shift-save-list-2026-09-06.jpg)
retains the old `LIVING QUARTERS OUTBOUND` slot, Sep 06 04:53 timestamp, 00:38
elapsed time and tram preview. Native Load Game restores `save/save000.sav`
and `save/ba_tram1.HL1` at 05:01:59; `Game started` follows at 05:02:00 and
resource propagation completes at 05:02:01. The
[restored world](goldsource-release-installed-blue-shift-restored-2026-09-06.jpg)
is textured, has 100 health and continues the tram journey. The
[native log](goldsource-release-installed-blue-shift-restored-2026-09-06.json)
contains fresh engine initialization and load, not a new save or an in-memory
carryover. The old compatibility slot was not overwritten.

Escape pauses normally. The same native Save/load menu now displays both
[F6 and F7](goldsource-release-installed-blue-shift-hints-2026-09-06.jpg), replacing
the reproduced old `<KEY NOT FOUND>` labels. Pointer capture remains false;
this release does not claim to resolve the separate browser capture failure.

After native `status` confirms `ba_tram1`, `disconnect` completes at 05:03:50.
A full page reload and normal Play → New Game → Medium starts a fresh campaign:
native `Host_EndGame` at 05:04:48 → `Game started` and resource completion at
05:04:49. The [fresh launch record](goldsource-release-installed-blue-shift-fresh-world-2026-09-06.json)
and [textured tram](goldsource-release-installed-blue-shift-fresh-world-2026-09-06.jpg)
confirm the repaired loading path on the live origin. The comparable old
native map-start stage took 20 seconds; the new one takes about 1 second.
Both use 1424×1057. These are whole-second native log timestamps, not a
subsecond/FPS benchmark or a claim that all cold asset downloads take one second.

Opposing Force's normal Play → New Game → Medium also reaches its
[textured helicopter intro](goldsource-release-installed-opposing-force-helicopter-2026-09-06.jpg),
with soldiers and exterior terrain visible after the expected title fade.
Native start logs record `Host_EndGame` at 05:06:57 and 05:06:58,
`Game started` at 05:06:58 and resource completion at 05:07:00—about 0–1
seconds for map start and 2–3 seconds through resources, depending on the
duplicate reset marker. The earlier `...-opposing-force-world` capture is
only the black title/fade screen; it is not used as renderer acceptance.

Half-Life also starts via normal New Game → Medium (`Game started` 05:09:59,
resources complete 05:10:00). Escape pauses and the native Resume game button
returns to the [textured tram route](goldsource-release-installed-half-life-tram-2026-09-06.jpg),
with rails, tunnel lights and a distant scientist visible. Its early
`...-half-life-world` image is the expected title fade, not the accepted
world capture. This is a startup/pause/resume check, not another full intro
transition or campaign run.

Counter-Strike's native Join Game connects to the unchanged managed host;
the [joined world](goldsource-release-installed-counter-strike-world-2026-09-06.jpg)
is textured and shows native team selection. Auto-select team/appearance
works. The immediately following `...-counter-strike-spawn` image actually
shows the waiting/spectator view on `de_dust`, not a first-person spawn, and
is not used as spawn acceptance. No host restart, map vote or fault injection
was performed.

The next round does produce a first-person pistol/HUD. The native Resume game
click leaves the menu open, reproducing the earlier CS-specific resume issue;
Escape successfully returns to the
[first-person world](goldsource-release-installed-counter-strike-escape-resume-2026-09-06.jpg)
with 86 health and 20/442 ammunition. Thus Escape resume and a real spawn are
verified, but the Resume button is **not** marked fixed. Native bot combat
continues while the menu is open. Irrelevant CS menu controls also remain.

The [native status response](goldsource-release-installed-counter-strike-roster-2026-09-06.json)
confirms `de_dust`, the preserved `ChromeCSReconnect` player and nine explicit
`Bot` rows. The server's `players: 1 active` field counts the human separately.
Use lowercase `status`: the initial uppercase spelling is rejected by the
remote CS command path, despite working for local single-player status.

Native `disconnect` at 05:16:15 clears server identity. Escape then returns to
[Join Game](goldsource-release-installed-counter-strike-disconnected-menu-2026-09-06.jpg)
before the owned tab is cleared. The returned menu has a black background and
missing Console label; menu rendering/controls remain a separate follow-up.
The [combined evidence audit](goldsource-release-chrome-2026-09-06.json) checks
the saved observations, image identity, old-save/deployment/restore ordering,
native timing, nine-bot roster and clean departures. Screenshots were manually
reviewed; the script does not claim to test their appearance by OCR.

## Rerun and rollback

From `goldsource-wasm`:

```sh
npm test
node scripts/test-release-package.mjs --installed
node scripts/test-release-evidence.mjs
```

The pre-release mode `--record-before` is write-once and requires the old image.
Do not overwrite the baseline after deployment. Historical one-change package
checks retain their original candidate-specific scope.

Rollback image `c20b2d218bc26c41b040863616a3d29beb5f2dd72ff53a6154558d9b5530fdcd`
is retained as `local/goldsource-wasm:release-baseline-20260906`. Restore only
GoldSource's image contract/tag and old data bind before recreating that one
frontend with `--no-deps`. Preserve both data directories and the browser
saves. No browser-storage export, edit or hidden engine call was used.

Capture/mouse look, held controls, audible playback, wider saves/campaigns,
CS Resume-button/menu rendering/model overflow and the existing native GL/media/config warnings
remain open. Blood's crash remains user-deferred; RTCW SP is untouched.
